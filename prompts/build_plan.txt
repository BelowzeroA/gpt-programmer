You have a task to perform on a table. Now you need to build a general plan for the task.
The plan is a chain of operations that you will need to perform on each row of the table.
Some operation require external plugins, for instance a pluging to read a webpage from a link.
You are given a list of available plugins, list of columns and two first rows of the table.
When there are multiple operations, some operation may require the output of previous operations.

Answer in the following format (JSONL):
1. { "operation": "action to perform", "plugin": "plugin1", "input_source": "PreviousOperation|Table", "columns": ["column1", "column2"] }
2. { "operation": "action to perform", "plugin": null, "input_source": "PreviousOperation|Table", "columns": ["column1"] }
...

Plugins:
{ "name": "read_url", "description": "Brings page content from the url", "parameters": ["url", "max_length"] }
{ "name": "chat_gpt", "description": "Performs any text to text task", "parameters": ["input", "max_length"] }
{ "name": "google_search", "description": "Searches google for the specified text and returns first N first urls", parameters: ["query", "num_results"] }

Examples:
Task:
"Take the person mentioned in the column text and put it in the column person"
Columns:
["Text", "format", "L.no"]
Plan:
1. { "operation": "Extract a person name from text", "plugin": "chat_gpt", "input_source": "Table", "columns": ["Text"] }
2. { "operation": "Insert a person name into a new column", "plugin": null, "input_source": "PreviousOperation", "columns": ["Person"] }
---

Task:
"For each row, read the webpage from column 'link', extract a company name from the webpage content and put the company name in the column company"
Columns:
["db_ref", "Link", "responsible"]
Plan:
1. { "operation": "Read the webpage content", "plugin": "read_url", "input_source": "Table",  "columns": ["Link"] }
2. { "operation": "Extract a company name from text", "plugin": "chat_gpt", "input_source": "PreviousOperation", "columns": null }
3. { "operation": "Insert a company name into a new column", "plugin": null, "input_source": "PreviousOperation", "columns": ["Company"] }
---

Now the real data:
Task:
"[task]"
Columns:
[columns]
Plan: