You are given a task from your manager to implement a simple program as a part of project, specified by a user.
You need to follow the step-by-step plan and write the code to implement the program.

Project specification from user:
```

    Write a parser of dataset in json format. The dataset consists of multiple files in a directory. Each file contains some code in multiple languages. The structure of the files is unknown, you need to determine it first.
    The task is to write a python module that extracts all code in java and put it in a single csv file with the following columns: repo_name, full_path, content.
    The end result is a python module that implements the above task and stores the result in file "dataset.csv".
    
```

Step-by-step plan:
```
{1: 'Investigate the directory structure and file formats of the dataset to determine how to parse the JSON files and extract the code snippets.', 2: 'Develop a Python module that:\n- Recursively traverses the dataset directory and reads each JSON file\n- Extracts all Java code snippets from the JSON files\n- Stores the extracted code snippets in a CSV file with columns: repo_name, full_path, content\n- Handles any parsing errors or unexpected JSON structures gracefully', 3: 'Test the Python module on a subset of the dataset to ensure it correctly extracts and stores the Java code snippets in the CSV file format.', 4: 'Run the Python module on the entire dataset and generate the final "dataset.csv" file containing all extracted Java code snippets.', 5: 'Verify the "dataset.csv" file contains the expected data and is structured correctly with the specified columns.\n```'}
```

Current step you are implementing:
```
Investigate the directory structure and file formats of the dataset to determine how to parse the JSON files and extract the code snippets.
```

Task clarification from your manager:
```
Modify the Python script to handle the case when a value in the JSON structure is a list. Instead of trying to access structure[key] when the value is a list, append the type name to the list in the structure.
```

On the previous step, you took some actions and made observations that will help you to implement the current step.
```

Observation #1
Task: "Write a Python script to investigate the directory structure and file formats of the dataset located at 'c:/Work/projects/data/'. Determine how to parse the JSON files and extract the code snippets in Java."
Result:
---
{'code': 'import os\nimport json\n\ndef investigate_json_structure(json_data, level=1, structure=None):\n    if structure is None:\n        structure = {}\n\n    if level <= 3:\nError here >>> for key, value in json_data.items():\n            if isinstance(value, dict):\n                structure[key] = {}\n                investigate_json_structure(value, level + 1, structure[key])\n            elif isinstance(value, list):\n                structure[key] = []\n                if value:\n                    investigate_json_structure(value[0], level + 1, structure[key])\n            else:\n                structure[key] = type(value).__name__\n\n    return structure\n\ndef main():\n    dataset_dir = \'c:/Work/projects/data/\'\n    report_file = \'report.txt\'\n\n    # Investigate directory structure\n    dir_structure = []\n    for root, dirs, files in os.walk(dataset_dir):\n        dir_structure.append(f"Directory: {root}")\n        dir_structure.append(f"  Subdirectories: {dirs}")\n        dir_structure.append(f"  Files: {files}")\n\n    # Investigate JSON file structure\n    json_files = [file for file in os.listdir(dataset_dir) if file.endswith(\'.json\')]\n    json_structure = {}\n    for json_file in json_files[:3]:\n        file_path = os.path.join(dataset_dir, json_file)\n        with open(file_path, \'r\') as f:\n            json_data = json.load(f)\n            json_structure[json_file] = investigate_json_structure(json_data)\n\n    # Write investigation results to report file\n    with open(report_file, \'w\') as f:\n        f.write("Directory Structure:\\n")\n        f.write("\\n".join(dir_structure))\n        f.write("\\n\\nJSON File Structure:\\n")\n        f.write(json.dumps(json_structure, indent=2))\n\nif __name__ == \'__main__\':\n    main()', 'code_run_output': 'Traceback (most recent call last):\r\n  File "C:\\Work\\projects\\gpt-programmer\\stage\\temp_module.py", line 50, in <module>\r\n    main()\r\n  File "C:\\Work\\projects\\gpt-programmer\\stage\\temp_module.py", line 40, in main\r\n    json_structure[json_file] = investigate_json_structure(json_data)\r\n                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\r\n  File "C:\\Work\\projects\\gpt-programmer\\stage\\temp_module.py", line 9, in investigate_json_structure\r\n    for key, value in json_data.items():\r\n                      ^^^^^^^^^^^^^^^\r\nAttributeError: \'list\' object has no attribute \'items\'\r\n'}
---

Observation #2
Task: "Fix the error in the Python script that investigates the directory structure and file formats of the JSON dataset. The error occurs when trying to call .items() on a list object. Modify the script to handle both dictionaries and lists in the JSON structure."
Result:
---
{'code': 'import os\nimport json\n\ndef investigate_json_structure(json_data, level=1, structure=None):\n    if structure is None:\n        structure = {}\n\n    if level <= 3:\n        if isinstance(json_data, dict):\n            for key, value in json_data.items():\n                if isinstance(value, dict):\n                    structure[key] = {}\n                    investigate_json_structure(value, level + 1, structure[key])\n                elif isinstance(value, list):\n                    structure[key] = []\n                    if value:\n                        investigate_json_structure(value[0], level + 1, structure[key])\n                else:\nError here >>> structure[key] = type(value).__name__\n        elif isinstance(json_data, list):\n            if json_data:\n                investigate_json_structure(json_data[0], level, structure)\n\n    return structure\n\ndef main():\n    dataset_dir = \'c:/Work/projects/data/\'\n    report_file = \'report.txt\'\n\n    # Investigate directory structure\n    dir_structure = []\n    for root, dirs, files in os.walk(dataset_dir):\n        dir_structure.append(f"Directory: {root}")\n        dir_structure.append(f"  Subdirectories: {dirs}")\n        dir_structure.append(f"  Files: {files}")\n\n    # Investigate JSON file structure\n    json_files = [file for file in os.listdir(dataset_dir) if file.endswith(\'.json\')]\n    json_structure = {}\n    for json_file in json_files[:3]:\n        file_path = os.path.join(dataset_dir, json_file)\n        with open(file_path, \'r\') as f:\n            json_data = json.load(f)\n            json_structure[json_file] = investigate_json_structure(json_data)\n\n    # Write investigation results to report file\n    with open(report_file, \'w\') as f:\n        f.write("Directory Structure:\\n")\n        f.write("\\n".join(dir_structure))\n        f.write("\\n\\nJSON File Structure:\\n")\n        f.write(json.dumps(json_structure, indent=2))\n\nif __name__ == \'__main__\':\n    main()', 'code_run_output': 'Traceback (most recent call last):\r\n  File "C:\\Work\\projects\\gpt-programmer\\stage\\temp_module.py", line 54, in <module>\r\n    main()\r\n  File "C:\\Work\\projects\\gpt-programmer\\stage\\temp_module.py", line 44, in main\r\n    json_structure[json_file] = investigate_json_structure(json_data)\r\n                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\r\n  File "C:\\Work\\projects\\gpt-programmer\\stage\\temp_module.py", line 22, in investigate_json_structure\r\n    investigate_json_structure(json_data[0], level, structure)\r\n  File "C:\\Work\\projects\\gpt-programmer\\stage\\temp_module.py", line 17, in investigate_json_structure\r\n    investigate_json_structure(value[0], level + 1, structure[key])\r\n  File "C:\\Work\\projects\\gpt-programmer\\stage\\temp_module.py", line 19, in investigate_json_structure\r\n    structure[key] = type(value).__name__\r\n    ~~~~~~~~~^^^^^\r\nTypeError: list indices must be integers or slices, not str\r\n'}
---

```

Read the step-by-step plan carefully. Think step-by-step.
Then write the code to implement the current step of the plan.

Your response should only be Python code.

Rules:
- Make executable python code (it must contain main() function)

- To investigate something, store the result of the investigation in file "report.txt" so that the next AI agent would be able to continue the work

- To investigate json file structure, collect keys and types of values from the the first three levels of the structure and store them in json format. If an element is a list, dive in and discover the structure of the first element


Never provide any explanation or context inside the response, only the code.